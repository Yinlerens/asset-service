apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: asset-service
  namespace: argocd
  labels:
    app.kubernetes.io/name: asset-service
    app.kubernetes.io/part-of: asset-service
spec:
  project: default
  source:
    repoURL: https://github.com/Yinlerens/asset-service.git
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: asset-service
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
